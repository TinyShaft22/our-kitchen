---
phase: 01-foundation
plan: 02
type: execute
---

<objective>
Set up React Router with bottom navigation tabs matching the app's four main sections.

Purpose: Establish the navigation shell that all pages will plug into.
Output: Working bottom tab navigation with Home, Meals, Grocery, and Baking routes.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/SPEC.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

**Navigation structure from SPEC.md:**
1. Home - This week's meals + grocery status
2. Meals - Meal library
3. Grocery - Full list by store
4. Baking - Bella's corner

**Design requirements:**
- Bottom tabs (mobile-first, 44px min touch targets)
- Warm & cozy theme colors
- Rounded corners, soft shadows
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install React Router and create page placeholders</name>
  <files>package.json, src/pages/Home.tsx, src/pages/MealLibrary.tsx, src/pages/GroceryListPage.tsx, src/pages/BakingPage.tsx</files>
  <action>
Install: `npm install react-router-dom`

Create src/pages/ directory with four placeholder pages:
- Home.tsx: Simple component returning "Home - This Week's Meals"
- MealLibrary.tsx: "Meal Library"
- GroceryListPage.tsx: "Grocery List"
- BakingPage.tsx: "Baking Corner"

Each page should be a functional component with TypeScript, using Tailwind classes for basic styling (p-4, text-xl, font-semibold).
  </action>
  <verify>Files exist, no TypeScript errors</verify>
  <done>Four page components created in src/pages/</done>
</task>

<task type="auto">
  <name>Task 2: Create Navigation component with bottom tabs</name>
  <files>src/components/layout/Navigation.tsx</files>
  <action>
Create src/components/layout/ directory structure.

Build Navigation.tsx:
- Fixed bottom position (fixed bottom-0 left-0 right-0)
- White background with soft shadow (shadow-soft)
- Safe area padding for iOS (pb-safe or manual padding)
- Four tab buttons in a flex row
- Each tab: icon placeholder + label, 44px min height for touch target
- Active tab: terracotta color, inactive: charcoal with opacity
- Use NavLink from react-router-dom for active state detection

Tab structure:
1. Home (icon: house/home)
2. Meals (icon: utensils/book)
3. Grocery (icon: shopping-cart/list)
4. Baking (icon: cake/cookie)

For now, use emoji or text as icon placeholders (🏠 🍽️ 🛒 🧁) - real icons can be added later.
  </action>
  <verify>Component renders without errors, TypeScript compiles</verify>
  <done>Navigation component with four bottom tabs, proper styling, NavLink active states</done>
</task>

<task type="auto">
  <name>Task 3: Wire up App.tsx with Router and Navigation</name>
  <files>src/App.tsx</files>
  <action>
Update App.tsx to:
- Import BrowserRouter, Routes, Route from react-router-dom
- Import Navigation and all four page components
- Wrap app in BrowserRouter
- Create layout with:
  - Main content area (flex-1, overflow-auto, pb-20 for nav clearance)
  - Navigation component at bottom
- Define routes:
  - "/" → Home
  - "/meals" → MealLibrary
  - "/grocery" → GroceryListPage
  - "/baking" → BakingPage

Structure:
```
<BrowserRouter>
  <div className="min-h-screen bg-cream flex flex-col">
    <main className="flex-1 overflow-auto pb-20">
      <Routes>...</Routes>
    </main>
    <Navigation />
  </div>
</BrowserRouter>
```
  </action>
  <verify>npm run dev shows app with bottom navigation, clicking tabs navigates between pages</verify>
  <done>Full routing works, navigation shows active state, pages render correctly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts without errors
- [ ] `npm run build` succeeds
- [ ] Bottom navigation appears fixed at bottom
- [ ] Clicking each tab navigates to correct page
- [ ] Active tab shows terracotta color
- [ ] Touch targets are adequate size (visually check 44px height)
</verification>

<success_criteria>
- React Router installed and configured
- Four page placeholder components exist
- Bottom tab navigation works with proper active states
- Layout structure ready for content
- No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`

Phase 1 complete when this plan finishes. Update STATE.md and ROADMAP.md progress.
</output>
