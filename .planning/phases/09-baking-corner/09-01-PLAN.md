---
phase: 09-baking-corner
plan: 01
type: execute
---

<objective>
Create baking inventory display with status-based filtering.

Purpose: Show Bella's baking essentials with clear status visibility (stocked/low/out) and ability to filter by status.
Output: BakingEssentialCard component, full BakingPage with inventory list and status filter pills.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context:
@.planning/phases/08-voice-staples/08-01-SUMMARY.md

# Existing code:
@src/types/index.ts
@src/hooks/useBaking.ts
@src/pages/BakingPage.tsx
@src/components/grocery/StapleCard.tsx
@src/components/grocery/GroceryItemCard.tsx
@src/pages/GroceryListPage.tsx

**Tech stack available:** React, TypeScript, Tailwind, Firebase/Firestore, useBaking hook
**Established patterns:** Status badges, filter pills (GroceryListPage), card components (StapleCard, GroceryItemCard)
**Key files:** useBaking hook exists with CRUD + lowStockItems computed value, BakingEssential type defined
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BakingEssentialCard component</name>
  <files>src/components/baking/BakingEssentialCard.tsx</files>
  <action>
Create BakingEssentialCard component displaying baking essential info with status indicator.

Structure:
- Display: name, qty + unit (e.g. "2 cups"), status badge
- Status badge colors:
  - stocked: sage green bg (bg-sage/20 text-sage)
  - low: honey/warning (bg-honey/20 text-honey-dark)
  - out: terracotta/red (bg-terracotta/20 text-terracotta)
- Status text: "Stocked", "Low", "Out"
- Use rounded-soft, bg-white, shadow-soft styling
- Min height 44px for touch target
- Include edit (pencil emoji) and delete (trash emoji) buttons that appear on tap/hover

Props:
- essential: BakingEssential
- onEdit: () => void
- onDelete: () => void
- onStatusChange: (status: BakingStatus) => void

Add status cycling on badge tap: stocked -> low -> out -> stocked (quick status update without modal).
  </action>
  <verify>Component renders without errors, TypeScript compiles clean</verify>
  <done>BakingEssentialCard displays essential info with status badge and action buttons</done>
</task>

<task type="auto">
  <name>Task 2: Implement BakingPage with inventory list and status filter</name>
  <files>src/pages/BakingPage.tsx</files>
  <action>
Implement full BakingPage with inventory display and status filtering.

**Header section:**
- Title: "Baking Corner" with cupcake emoji
- Subtitle showing item counts: "X items (Y need restocking)"

**Status filter pills (below header):**
- Filter options: All, Stocked, Low, Out
- Style like store filter pills in GroceryListPage
- Active filter: bg-terracotta text-white
- Inactive: bg-charcoal/10 text-charcoal
- "All" selected by default

**Inventory list:**
- Use useBaking hook to get essentials
- Filter by selected status (or show all)
- Sort order: out first, then low, then stocked (urgency order)
- Display BakingEssentialCard for each item
- Empty state: "No baking essentials yet. Add some to get started!"

**Loading/error states:**
- Show loading spinner while loading
- Show error message if error

Wire up card handlers:
- onEdit: placeholder (will implement in 09-02)
- onDelete: placeholder (will implement in 09-02)
- onStatusChange: call updateStatus from useBaking hook

Import useHousehold to get householdCode for useBaking.
  </action>
  <verify>npm run build passes, page displays with filter pills, items sorted by status urgency</verify>
  <done>BakingPage shows inventory with working status filter and status cycling on card tap</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] BakingEssentialCard renders with name, qty/unit, status badge
- [ ] Status badge shows correct color per status
- [ ] Tapping status badge cycles through statuses
- [ ] BakingPage shows status filter pills
- [ ] Filter correctly shows only matching items
- [ ] Items sorted by urgency (out > low > stocked)
- [ ] Empty state shows when no items
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- Baking inventory is visible and filterable by status
</success_criteria>

<output>
After completion, create `.planning/phases/09-baking-corner/09-01-SUMMARY.md`
</output>
